type Difficulty = 
 record {
   height: nat;
   mines: nat;
   width: nat;
 };
service : {
  getDifficulty: (nat) -> (Difficulty) query;
  getHighScore: () -> (nat) query;
  updateHighScore: (nat) -> (nat);
}
